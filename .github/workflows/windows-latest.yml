# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         fuke --generate-configuration GitHubActions_windows-latest --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: windows-latest

on:
  push:
    branches-ignore:
      - main
      - 'release/*'
  pull_request:
    branches:
      - develop

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: 'Cache: .fuke/temp, ~/.nuget/packages'
        uses: actions/cache@v6
        with:
          path: |
            .fuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Test, Pack'
        run: ./build.cmd Test Pack
